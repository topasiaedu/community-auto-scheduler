/**
 * WhatsApp single-message text limits (aligned with WhatsApp Web / Business API behavior).
 * Image posts use a separate caption limit; see WHATSAPP_IMAGE_CAPTION_MAX_CHARS.
 */

/** Max characters for a standard chat message body / image caption in WhatsApp. */
export const WHATSAPP_POST_TEXT_MAX_CHARS = 4096;
