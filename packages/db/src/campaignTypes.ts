/**
 * JSON shape stored on `Campaign.customValues` (8 merge fields from P7 UX spec §4 Step 1).
 */
export interface CampaignCustomValues {
  workshopDay: string;
  workshopDate: string;
  workshopTime: string;
  zoomLink: string;
  sessionDate: string;
  sessionTime: string;
  zoomId: string;
  zoomPasscode: string;
}
