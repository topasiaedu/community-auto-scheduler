{
  "name": "community-auto-scheduler",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n api,web -c blue,green \"npm run dev -w @nmcas/api\" \"npm run dev -w @nmcas/web\"",
    "build:packages": "npm run build -w @nmcas/db && npm run build -w @nmcas/wa-session-storage",
    "build:api": "npm run build:packages && npm run build -w @nmcas/api",
    "build:web": "npm run build -w @nmcas/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:generate": "npm run generate -w @nmcas/db",
    "db:migrate": "npm run migrate:dev -w @nmcas/db",
    "db:deploy": "node ./scripts/migrate-deploy.mjs",
    "db:seed": "node ./scripts/seed.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7",
    "typescript": "^5.7.3"
  }
}
